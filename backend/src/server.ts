import 'dotenv/config';
import express, { NextFunction, Request, Response } from 'express';
import cors from 'cors';
import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';
import { PrismaClient, Role, MovementType, ChallanStatus, CustomerStatus, CustomerType } from '@prisma/client';
import { z } from 'zod';
const prisma=new PrismaClient(); const app=express(); app.use(cors({origin:process.env.CORS_ORIGIN?process.env.CORS_ORIGIN.split(','):true})); app.use(express.json({limit:'2mb'}));
type AuthRequest=Request&{user?:{id:string;role:Role;name:string;email:string}}; const JWT_SECRET=process.env.JWT_SECRET||'dev-only-change-me'; const sign=(id:string)=>jwt.sign({sub:id},JWT_SECRET,{expiresIn:'8h'}); const safeUser=(u:any)=>({id:u.id,name:u.name,email:u.email,role:u.role});
const auth=async(req:AuthRequest,res:Response,next:NextFunction)=>{try{const h=req.headers.authorization;if(!h?.startsWith('Bearer '))return res.status(401).json({success:false,message:'Authentication required'});const p=jwt.verify(h.slice(7),JWT_SECRET) as jwt.JwtPayload;const u=await prisma.user.findUnique({where:{id:String(p.sub)},select:{id:true,role:true,name:true,email:true}});if(!u)return res.status(401).json({success:false,message:'Invalid authentication token'});req.user=u;next()}catch{return res.status(401).json({success:false,message:'Invalid or expired token'})}};
const allow=(...roles:Role[])=>(req:AuthRequest,res:Response,next:NextFunction)=>{if(!req.user||!roles.includes(req.user.role))return res.status(403).json({success:false,message:'Insufficient permissions'});next()}; const pagination=(req:Request)=>({page:Math.max(1,Number(req.query.page||1)),limit:Math.min(100,Math.max(1,Number(req.query.limit||10)))}); const dateOrUndefined=(v?:string)=>v?new Date(v):undefined;
app.get('/health',(_req,res)=>res.json({status:'ok',service:'erp-crm-api'}));
app.post('/auth/login',async(req,res)=>{const p=z.object({email:z.string().email(),password:z.string().min(1)}).safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Invalid login data',errors:p.error.flatten()});const u=await prisma.user.findUnique({where:{email:p.data.email.toLowerCase()}});if(!u||!(await bcrypt.compare(p.data.password,u.passwordHash)))return res.status(401).json({success:false,message:'Invalid email or password'});res.json({success:true,token:sign(u.id),user:safeUser(u)})});
app.get('/auth/me',auth,async(req:AuthRequest,res)=>res.json({success:true,user:safeUser(req.user)}));
app.get('/dashboard/summary',auth,async(_req,res)=>{const [customers,products,drafts,confirmed,movements,productRows]=await Promise.all([prisma.customer.count(),prisma.product.count(),prisma.challan.count({where:{status:ChallanStatus.DRAFT}}),prisma.challan.count({where:{status:ChallanStatus.CONFIRMED}}),prisma.stockMovement.findMany({take:8,orderBy:{createdAt:'desc'},include:{product:{select:{name:true,sku:true}},createdBy:{select:{name:true}}}}),prisma.product.findMany({select:{currentStock:true,minimumStock:true}})]);const lowStock=productRows.filter(p=>p.currentStock<=p.minimumStock).length;res.json({success:true,data:{customers,products,lowStock,pendingChallans:drafts,confirmedChallans:confirmed,recentMovements:movements}})});
const customerSchema=z.object({customerName:z.string().min(2),mobile:z.string().min(5),email:z.string().email().optional().or(z.literal('')),businessName:z.string().min(2),gstNumber:z.string().optional(),customerType:z.nativeEnum(CustomerType),address:z.string().min(2),status:z.nativeEnum(CustomerStatus).default(CustomerStatus.LEAD),followUpDate:z.string().optional(),notes:z.string().optional()});
app.get('/customers',auth,async(req,res)=>{const {page,limit}=pagination(req),q=String(req.query.search||''),status=req.query.status as CustomerStatus|undefined;const where:any={...(status&&Object.values(CustomerStatus).includes(status)?{status}:{}),...(q?{OR:[{customerName:{contains:q,mode:'insensitive'}},{businessName:{contains:q,mode:'insensitive'}},{mobile:{contains:q}},{email:{contains:q,mode:'insensitive'}}]}:{})};const [data,total]=await Promise.all([prisma.customer.findMany({where,skip:(page-1)*limit,take:limit,orderBy:{createdAt:'desc'},include:{followUps:{take:1,orderBy:{createdAt:'desc'}}}}),prisma.customer.count({where})]);res.json({success:true,data,pagination:{page,limit,total,pages:Math.ceil(total/limit)}})});
app.post('/customers',auth,allow(Role.ADMIN,Role.SALES),async(req:AuthRequest,res)=>{const p=customerSchema.safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed',errors:p.error.flatten()});const c=await prisma.customer.create({data:{...p.data,email:p.data.email||null,followUpDate:dateOrUndefined(p.data.followUpDate),createdById:req.user!.id}});res.status(201).json({success:true,data:c})});
app.get('/customers/:id',auth,async(req,res)=>{const c=await prisma.customer.findUnique({where:{id:req.params.id},include:{followUps:{orderBy:{createdAt:'desc'},include:{createdBy:{select:{name:true,role:true}}}},challans:{orderBy:{createdAt:'desc'},take:10}}});if(!c)return res.status(404).json({success:false,message:'Customer not found'});res.json({success:true,data:c})});
app.put('/customers/:id',auth,allow(Role.ADMIN,Role.SALES),async(req,res)=>{const p=customerSchema.partial().safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed',errors:p.error.flatten()});try{const data:any={...p.data};if('email'in data)data.email=data.email||null;if(data.followUpDate)data.followUpDate=new Date(data.followUpDate);const c=await prisma.customer.update({where:{id:req.params.id},data});res.json({success:true,data:c})}catch{res.status(404).json({success:false,message:'Customer not found'})}});
app.post('/customers/:id/followups',auth,allow(Role.ADMIN,Role.SALES),async(req:AuthRequest,res)=>{const p=z.object({note:z.string().min(1),followUpDate:z.string().optional()}).safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed'});const customer=await prisma.customer.findUnique({where:{id:req.params.id}});if(!customer)return res.status(404).json({success:false,message:'Customer not found'});const f=await prisma.followUp.create({data:{customerId:customer.id,note:p.data.note,followUpDate:dateOrUndefined(p.data.followUpDate),createdById:req.user!.id}});res.status(201).json({success:true,data:f})});
const productSchema=z.object({name:z.string().min(2),sku:z.string().min(1),category:z.string().min(1),unitPrice:z.coerce.number().nonnegative(),currentStock:z.coerce.number().int().nonnegative().default(0),minimumStock:z.coerce.number().int().nonnegative().default(0),warehouseLocation:z.string().min(1),imageUrl:z.string().url().optional().or(z.literal(''))});
app.get('/products',auth,async(req,res)=>{const {page,limit}=pagination(req),q=String(req.query.search||''),low=req.query.lowStock==='true';const all=await prisma.product.findMany({where:q?{OR:[{name:{contains:q,mode:'insensitive'}},{sku:{contains:q,mode:'insensitive'}},{category:{contains:q,mode:'insensitive'}}]}:{},orderBy:{createdAt:'desc'}});const filtered=low?all.filter(p=>p.currentStock<=p.minimumStock):all;res.json({success:true,data:filtered.slice((page-1)*limit,page*limit),pagination:{page,limit,total:filtered.length,pages:Math.ceil(filtered.length/limit)}})});
app.get('/products/:id',auth,async(req,res)=>{const p=await prisma.product.findUnique({where:{id:req.params.id},include:{movements:{orderBy:{createdAt:'desc'},take:50}}});if(!p)return res.status(404).json({success:false,message:'Product not found'});res.json({success:true,data:p})});
app.post('/products',auth,allow(Role.ADMIN),async(req:AuthRequest,res)=>{const p=productSchema.safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed',errors:p.error.flatten()});try{const result=await prisma.$transaction(async tx=>{const x=await tx.product.create({data:{...p.data,imageUrl:p.data.imageUrl||null}});if(x.currentStock>0)await tx.stockMovement.create({data:{productId:x.id,quantity:x.currentStock,movementType:MovementType.IN,reason:'Opening stock',createdById:req.user!.id}});return x});res.status(201).json({success:true,data:result})}catch{res.status(409).json({success:false,message:'SKU already exists'})}});
app.put('/products/:id',auth,allow(Role.ADMIN),async(req,res)=>{const p=productSchema.partial().safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed'});try{const x=await prisma.product.update({where:{id:req.params.id},data:{...p.data,imageUrl:p.data.imageUrl===''?null:p.data.imageUrl}});res.json({success:true,data:x})}catch{res.status(404).json({success:false,message:'Product not found'})}});
app.post('/products/:id/stock',auth,allow(Role.ADMIN,Role.WAREHOUSE),async(req:AuthRequest,res)=>{const p=z.object({quantity:z.coerce.number().int().positive(),movementType:z.nativeEnum(MovementType),reason:z.string().min(2)}).safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed',errors:p.error.flatten()});try{const result=await prisma.$transaction(async tx=>{const product=await tx.product.findUnique({where:{id:req.params.id}});if(!product)throw new Error('NOT_FOUND');if(p.data.movementType===MovementType.OUT&&product.currentStock<p.data.quantity)throw new Error('INSUFFICIENT');const stock=p.data.movementType===MovementType.IN?product.currentStock+p.data.quantity:product.currentStock-p.data.quantity;const updated=await tx.product.update({where:{id:product.id},data:{currentStock:stock}});const movement=await tx.stockMovement.create({data:{productId:product.id,quantity:p.data.quantity,movementType:p.data.movementType,reason:p.data.reason,createdById:req.user!.id}});return{updated,movement}});res.json({success:true,data:result})}catch(e:any){if(e.message==='NOT_FOUND')return res.status(404).json({success:false,message:'Product not found'});if(e.message==='INSUFFICIENT')return res.status(409).json({success:false,message:'Insufficient stock'});res.status(500).json({success:false,message:'Could not update stock'})}});
app.get('/products/:id/stock-movements',auth,async(req,res)=>{const data=await prisma.stockMovement.findMany({where:{productId:req.params.id},orderBy:{createdAt:'desc'},include:{createdBy:{select:{name:true,role:true}}}});res.json({success:true,data})});
app.get('/stock-movements',auth,async(req,res)=>{const {page,limit}=pagination(req);const [data,total]=await Promise.all([prisma.stockMovement.findMany({skip:(page-1)*limit,take:limit,orderBy:{createdAt:'desc'},include:{product:true,createdBy:{select:{name:true,role:true}}}}),prisma.stockMovement.count()]);res.json({success:true,data,pagination:{page,limit,total,pages:Math.ceil(total/limit)}})});
const challanSchema=z.object({customerId:z.string().min(1),items:z.array(z.object({productId:z.string().min(1),quantity:z.coerce.number().int().positive()})).min(1)});
app.get('/challans',auth,async(req,res)=>{const {page,limit}=pagination(req),status=req.query.status as ChallanStatus|undefined,where=status&&Object.values(ChallanStatus).includes(status)?{status}:{};const [data,total]=await Promise.all([prisma.challan.findMany({where,skip:(page-1)*limit,take:limit,include:{customer:true,createdBy:{select:{name:true,role:true}},items:true},orderBy:{createdAt:'desc'}}),prisma.challan.count({where})]);res.json({success:true,data,pagination:{page,limit,total,pages:Math.ceil(total/limit)}})});
app.get('/challans/:id',auth,async(req,res)=>{const c=await prisma.challan.findUnique({where:{id:req.params.id},include:{customer:true,createdBy:{select:{name:true,role:true}},items:true}});if(!c)return res.status(404).json({success:false,message:'Challan not found'});res.json({success:true,data:c})});
app.post('/challans',auth,allow(Role.ADMIN,Role.SALES),async(req:AuthRequest,res)=>{const p=challanSchema.safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed',errors:p.error.flatten()});const customer=await prisma.customer.findUnique({where:{id:p.data.customerId}});if(!customer)return res.status(404).json({success:false,message:'Customer not found'});const products=await prisma.product.findMany({where:{id:{in:p.data.items.map(i=>i.productId)}}});const map=new Map(products.map(x=>[x.id,x]));const items=p.data.items.map(i=>{const x=map.get(i.productId);if(!x)throw new Error('PRODUCT');return{...i,productName:x.name,sku:x.sku,unitPrice:x.unitPrice}});const number=`CH-${new Date().toISOString().slice(0,10).replace(/-/g,'')}-${String(Date.now()).slice(-6)}`;const c=await prisma.challan.create({data:{challanNumber:number,customerId:customer.id,createdById:req.user!.id,totalQuantity:items.reduce((a,b)=>a+b.quantity,0),items:{create:items}},include:{items:true,customer:true}});res.status(201).json({success:true,data:c})});
app.put('/challans/:id',auth,allow(Role.ADMIN,Role.SALES),async(req,res)=>{const p=challanSchema.safeParse(req.body);if(!p.success)return res.status(422).json({success:false,message:'Validation failed'});try{const updated=await prisma.$transaction(async tx=>{const c=await tx.challan.findUnique({where:{id:req.params.id}});if(!c)throw new Error('NOT_FOUND');if(c.status!==ChallanStatus.DRAFT)throw new Error('STATUS');const products=await tx.product.findMany({where:{id:{in:p.data.items.map(i=>i.productId)}}});const map=new Map(products.map(x=>[x.id,x]));const items=p.data.items.map(i=>{const x=map.get(i.productId);if(!x)throw new Error('PRODUCT');return{...i,productName:x.name,sku:x.sku,unitPrice:x.unitPrice}});await tx.challanItem.deleteMany({where:{challanId:c.id}});return tx.challan.update({where:{id:c.id},data:{customerId:p.data.customerId,totalQuantity:items.reduce((a,b)=>a+b.quantity,0),items:{create:items}},include:{items:true,customer:true}})});res.json({success:true,data:updated})}catch(e:any){if(e.message==='NOT_FOUND')return res.status(404).json({success:false,message:'Challan not found'});if(e.message==='STATUS')return res.status(409).json({success:false,message:'Only draft challans can be edited'});res.status(422).json({success:false,message:'Invalid challan products'})}});
app.post('/challans/:id/confirm',auth,allow(Role.ADMIN,Role.SALES),async(req:AuthRequest,res)=>{try{const c=await prisma.$transaction(async tx=>{const challan=await tx.challan.findUnique({where:{id:req.params.id},include:{items:true}});if(!challan)throw new Error('NOT_FOUND');if(challan.status!==ChallanStatus.DRAFT)throw new Error('STATUS');const products=await tx.product.findMany({where:{id:{in:challan.items.map(i=>i.productId)}}});const map=new Map(products.map(p=>[p.id,p]));for(const i of challan.items){const p=map.get(i.productId);if(!p||p.currentStock<i.quantity)throw new Error(`STOCK:${i.productName}`)}for(const i of challan.items){const p=map.get(i.productId)!;await tx.product.update({where:{id:p.id},data:{currentStock:{decrement:i.quantity}}});await tx.stockMovement.create({data:{productId:p.id,quantity:i.quantity,movementType:MovementType.OUT,reason:`Sales challan ${challan.challanNumber}`,createdById:req.user!.id}})}return tx.challan.update({where:{id:challan.id},data:{status:ChallanStatus.CONFIRMED},include:{items:true,customer:true}})});res.json({success:true,data:c})}catch(e:any){if(e.message==='NOT_FOUND')return res.status(404).json({success:false,message:'Challan not found'});if(e.message==='STATUS')return res.status(409).json({success:false,message:'Only draft challans can be confirmed'});if(e.message.startsWith('STOCK:'))return res.status(409).json({success:false,message:`Insufficient stock for ${e.message.slice(6)}`});res.status(500).json({success:false,message:'Could not confirm challan'})}});
app.post('/challans/:id/cancel',auth,allow(Role.ADMIN,Role.SALES),async(req,res)=>{const c=await prisma.challan.findUnique({where:{id:req.params.id}});if(!c)return res.status(404).json({success:false,message:'Challan not found'});if(c.status===ChallanStatus.CONFIRMED)return res.status(409).json({success:false,message:'Confirmed challans cannot be cancelled'});const x=await prisma.challan.update({where:{id:c.id},data:{status:ChallanStatus.CANCELLED}});res.json({success:true,data:x})});
app.use((_req,res)=>res.status(404).json({success:false,message:'Route not found'}));app.use((err:any,_req:Request,res:Response,_next:NextFunction)=>{console.error(err);res.status(500).json({success:false,message:'Internal server error'})});const port=Number(process.env.PORT||4000);app.listen(port,()=>console.log(`ERP CRM API running on ${port}`));
